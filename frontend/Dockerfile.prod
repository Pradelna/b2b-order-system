FROM node:20-alpine AS build

WORKDIR /app

COPY ./package*.json ./
RUN npm install

COPY ./ .

RUN echo $VITE_API_URL

RUN npm run build

FROM alpine AS export
WORKDIR /result
COPY --from=build /app/dist ./html